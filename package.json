{
  "name": "pebble-sports-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for the Pebble Sports Simplified watchapp",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  }
}
